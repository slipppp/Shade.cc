-- SCRIPT PARA MOBILE - Chapéu Chinês ID: 78605226794898
-- Coloque em StarterPlayerScripts

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local head = character:WaitForChild("Head")

local hatModel = nil
local isHatEquipped = false
local isMenuOpen = false
local menuGui = nil
local dragData = nil
local hatAttachment = nil

-- FUNÇÃO PARA CARREGAR O ACESSÓRIO PELO ID
local function loadAccessoryById(id)
    local success, result = pcall(function()
        return game:GetService("MarketplaceService"):LoadAsset(id)
    end)
    
    if not success or not result then
        return nil
    end
    
    local hatModel = result:FindFirstChildWhichIsA("Model")
    if not hatModel then
        hatModel = result:FindFirstChildOfClass("Model")
    end
    
    if not hatModel then
        return nil
    end
    
    return hatModel
end

-- FUNÇÃO PARA CRIAR O CHAPÉU (USANDO O ID)
local function createHatFromID()
    local hatAsset = loadAccessoryById(78605226794898)
    
    if not hatAsset then
        return nil
    end
    
    -- Clonar o modelo
    local newHat = hatAsset:Clone()
    newHat.Name = "ChineseHat"
    
    -- Encontrar o Handle
    local handle = newHat:FindFirstChild("Handle")
    if not handle then
        handle = newHat:FindFirstChildWhichIsA("BasePart")
    end
    
    if not handle then
        newHat:Destroy()
        return nil
    end
    
    -- Criar Attachment na cabeça
    local headAttachment = Instance.new("Attachment")
    headAttachment.Name = "HatAttachment"
    headAttachment.Position = Vector3.new(0, 1.2, 0)
    headAttachment.Parent = head
    
    -- Criar Attachment no chapéu
    local hatAttach = Instance.new("Attachment")
    hatAttach.Name = "HatAttach"
    hatAttach.Parent = handle
    
    -- Criar WeldConstraint para manter FIRME
    local weld = Instance.new("WeldConstraint")
    weld.Name = "HatWeld"
    weld.Part0 = head
    weld.Part1 = handle
    weld.Parent = handle
    
    -- Posicionar corretamente
    local headCFrame = head.CFrame
    local hatCFrame = headCFrame * CFrame.new(0, 1.2, 0)
    handle.CFrame = hatCFrame
    
    -- Adicionar ao personagem
    newHat.Parent = character
    
    -- Guardar referência do handle
    hatAttachment = handle
    
    return newHat
end

-- FUNÇÃO PARA CRIAR CHAPÉU FALLBACK (CASO O ID NÃO FUNCIONE)
local function createFallbackHat()
    local model = Instance.new("Model")
    model.Name = "ChineseHat"
    
    local base = Instance.new("Part")
    base.Name = "Handle"
    base.Size = Vector3.new(3.5, 0.3, 3.5)
    base.Shape = Enum.PartType.Cylinder
    base.BrickColor = BrickColor.new("Bright yellow")
    base.Material = Enum.Material.Wood
    base.CanCollide = false
    base.Anchored = false
    base.Parent = model
    
    local top = Instance.new("Part")
    top.Name = "Top"
    top.Size = Vector3.new(2, 1.2, 2)
    top.Shape = Enum.PartType.Cylinder
    top.BrickColor = BrickColor.new("Bright yellow")
    top.Material = Enum.Material.Wood
    top.CanCollide = false
    top.Anchored = false
    top.Parent = model
    
    local ribbon = Instance.new("Part")
    ribbon.Name = "Ribbon"
    ribbon.Size = Vector3.new(3.3, 0.15, 0.25)
    ribbon.BrickColor = BrickColor.new("Bright red")
    ribbon.Material = Enum.Material.Fabric
    ribbon.CanCollide = false
    ribbon.Anchored = false
    ribbon.Parent = model
    
    -- Weld para manter FIRME
    local weld = Instance.new("WeldConstraint")
    weld.Name = "HatWeld"
    weld.Part0 = head
    weld.Part1 = base
    weld.Parent = base
    
    -- Posicionar
    local headCFrame = head.CFrame
    local hatCFrame = headCFrame * CFrame.new(0, 1.2, 0)
    
    base.CFrame = hatCFrame * CFrame.new(0, -0.1, 0)
    top.CFrame = hatCFrame * CFrame.new(0, 0.7, 0)
    ribbon.CFrame = hatCFrame * CFrame.new(0, 0.1, 0)
    
    model.Parent = character
    hatAttachment = base
    
    return model
end

-- EQUIPAR CHAPÉU
local function equipHat()
    if isHatEquipped then
        return
    end
    
    -- Tenta carregar pelo ID primeiro
    local hat = createHatFromID()
    
    -- Se falhar, usa o fallback
    if not hat then
        hat = createFallbackHat()
    end
    
    if hat then
        hatModel = hat
        isHatEquipped = true
    end
end

-- REMOVER CHAPÉU
local function removeHat()
    if hatModel then
        hatModel:Destroy()
        hatModel = nil
    end
    isHatEquipped = false
    hatAttachment = nil
end

-- TOGGLE
local function toggleHat()
    if isHatEquipped then
        removeHat()
    else
        equipHat()
    end
end

-- CRIAR GUI MÓVEL
local function createMobileGUI()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "HatMenuMobile"
    screenGui.ResetOnSpawn = false
    screenGui.Parent = player.PlayerGui
    
    -- FUNDO DO MENU
    local mainFrame = Instance.new("Frame")
    mainFrame.Name = "MainFrame"
    mainFrame.Size = UDim2.new(0, 320, 0, 450)
    mainFrame.Position = UDim2.new(0.5, -160, 0.5, -225)
    mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    mainFrame.BackgroundTransparency = 0.15
    mainFrame.BorderSizePixel = 0
    mainFrame.Visible = false
    mainFrame.Parent = screenGui
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 16)
    corner.Parent = mainFrame
    
    local stroke = Instance.new("UIStroke")
    stroke.Thickness = 2
    stroke.Color = Color3.fromRGB(255, 200, 50)
    stroke.Transparency = 0.6
    stroke.Parent = mainFrame
    
    -- BARRA DE TÍTULO
    local titleBar = Instance.new("Frame")
    titleBar.Name = "TitleBar"
    titleBar.Size = UDim2.new(1, 0, 0, 45)
    titleBar.BackgroundColor3 = Color3.fromRGB(255, 200, 50)
    titleBar.BackgroundTransparency = 0.3
    titleBar.BorderSizePixel = 0
    titleBar.Parent = mainFrame
    
    local titleCorner = Instance.new("UICorner")
    titleCorner.CornerRadius = UDim.new(0, 16)
    titleCorner.Parent = titleBar
    
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -60, 1, 0)
    titleLabel.Position = UDim2.new(0, 30, 0, 0)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = "🎩 Chapéu Chinês"
    titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    titleLabel.TextScaled = true
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.Parent = titleBar
    
    -- BOTÃO FECHAR
    local closeBtn = Instance.new("TextButton")
    closeBtn.Name = "CloseBtn"
    closeBtn.Size = UDim2.new(0, 35, 0, 35)
    closeBtn.Position = UDim2.new(1, -40, 0, 5)
    closeBtn.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeBtn.TextScaled = true
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.Parent = titleBar
    
    local closeCorner = Instance.new("UICorner")
    closeCorner.CornerRadius = UDim.new(0, 8)
    closeCorner.Parent = closeBtn
    
    -- BOTÃO TOGGLE
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Name = "ToggleBtn"
    toggleBtn.Size = UDim2.new(0.85, 0, 0, 60)
    toggleBtn.Position = UDim2.new(0.075, 0, 0.14, 0)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(255, 200, 50)
    toggleBtn.Text = "🔴 REMOVER CHAPÉU"
    toggleBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    toggleBtn.TextScaled = true
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.Parent = mainFrame
    
    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(0, 12)
    toggleCorner.Parent = toggleBtn
    
    -- CORES
    local colorLabel = Instance.new("TextLabel")
    colorLabel.Size = UDim2.new(1, 0, 0, 30)
    colorLabel.Position = UDim2.new(0, 0, 0.30, 0)
    colorLabel.BackgroundTransparency = 1
    colorLabel.Text = "🎨 CORES"
    colorLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    colorLabel.TextScaled = true
    colorLabel.Font = Enum.Font.GothamBold
    colorLabel.Parent = mainFrame
    
    local colorGrid = Instance.new("Frame")
    colorGrid.Size = UDim2.new(0.9, 0, 0, 80)
    colorGrid.Position = UDim2.new(0.05, 0, 0.37, 0)
    colorGrid.BackgroundTransparency = 1
    colorGrid.Parent = mainFrame
    
    local colorLayout = Instance.new("UIGridLayout")
    colorLayout.CellSize = UDim2.new(0, 40, 0, 40)
    colorLayout.CellPadding = UDim2.new(0, 8, 0, 8)
    colorLayout.FillDirection = Enum.FillDirection.Horizontal
    colorLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    colorLayout.Parent = colorGrid
    
    local cores = {
        Color3.fromRGB(218, 165, 32),
        Color3.fromRGB(255, 0, 0),
        Color3.fromRGB(0, 0, 255),
        Color3.fromRGB(0, 255, 0),
        Color3.fromRGB(0, 0, 0),
        Color3.fromRGB(255, 255, 255)
    }
    
    for _, cor in ipairs(cores) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, 40, 0, 40)
        btn.BackgroundColor3 = cor
        btn.BorderSizePixel = 2
        btn.BorderColor3 = Color3.fromRGB(255, 255, 255)
        btn.Text = ""
        btn.Parent = colorGrid
        
        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 20)
        btnCorner.Parent = btn
        
        local function changeColor()
            if hatModel then
                for _, part in ipairs(hatModel:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.BrickColor = BrickColor.new(cor)
                    end
                end
            end
        end
        
        btn.MouseButton1Click:Connect(changeColor)
        btn.TouchTap:Connect(changeColor)
    end
    
    -- MATERIAIS
    local matLabel = Instance.new("TextLabel")
    matLabel.Size = UDim2.new(1, 0, 0, 30)
    matLabel.Position = UDim2.new(0, 0, 0.56, 0)
    matLabel.BackgroundTransparency = 1
    matLabel.Text = "📦 MATERIAIS"
    matLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    matLabel.TextScaled = true
    matLabel.Font = Enum.Font.GothamBold
    matLabel.Parent = mainFrame
    
    local matGrid = Instance.new("Frame")
    matGrid.Size = UDim2.new(0.9, 0, 0, 60)
    matGrid.Position = UDim2.new(0.05, 0, 0.62, 0)
    matGrid.BackgroundTransparency = 1
    matGrid.Parent = mainFrame
    
    local matLayout = Instance.new("UIGridLayout")
    matLayout.CellSize = UDim2.new(0, 70, 0, 35)
    matLayout.CellPadding = UDim2.new(0, 8, 0, 8)
    matLayout.FillDirection = Enum.FillDirection.Horizontal
    matLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    matLayout.Parent = matGrid
    
    local materiais = {
        {Nome = "Madeira", Mat = Enum.Material.Wood},
        {Nome = "Plástico", Mat = Enum.Material.Plastic},
        {Nome = "Metal", Mat = Enum.Material.Metal},
        {Nome = "Neon", Mat = Enum.Material.Neon}
    }
    
    for _, mat in ipairs(materiais) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, 70, 0, 35)
        btn.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.Text = mat.Nome
        btn.TextScaled = true
        btn.Font = Enum.Font.GothamSemibold
        btn.Parent = matGrid
        
        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 8)
        btnCorner.Parent = btn
        
        local function changeMaterial()
            if hatModel then
                for _, part in ipairs(hatModel:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.Material = mat.Mat
                    end
                end
            end
        end
        
        btn.MouseButton1Click:Connect(changeMaterial)
        btn.TouchTap:Connect(changeMaterial)
    end
    
    -- BOTÃO FLUTUANTE
    local floatingBtn = Instance.new("TextButton")
    floatingBtn.Name = "FloatingBtn"
    floatingBtn.Size = UDim2.new(0, 70, 0, 70)
    floatingBtn.Position = UDim2.new(0.85, -35, 0.85, -35)
    floatingBtn.BackgroundColor3 = Color3.fromRGB(255, 200, 50)
    floatingBtn.Text = "🎩"
    floatingBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    floatingBtn.TextScaled = true
    floatingBtn.Font = Enum.Font.GothamBold
    floatingBtn.Parent = screenGui
    
    local floatCorner = Instance.new("UICorner")
    floatCorner.CornerRadius = UDim.new(0, 35)
    floatCorner.Parent = floatingBtn
    
    local floatStroke = Instance.new("UIStroke")
    floatStroke.Thickness = 3
    floatStroke.Color = Color3.fromRGB(255, 255, 255)
    floatStroke.Transparency = 0.5
    floatStroke.Parent = floatingBtn
    
    -- FUNÇÃO ARRASTAR
    local function makeDraggable(frame, button)
        local dragStart = nil
        local startPos = nil
        
        local function onInputBegan(input)
            if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
                dragStart = input.Position
                startPos = frame.Position
            end
        end
        
        local function onInputChanged(input)
            if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement then
                if dragStart and startPos then
                    local delta = input.Position - dragStart
                    local scaleX = delta.X / (frame.AbsoluteSize.X / frame.Size.X.Scale)
                    local scaleY = delta.Y / (frame.AbsoluteSize.Y / frame.Size.Y.Scale)
                    frame.Position = UDim2.new(
                        startPos.X.Scale + (delta.X / (frame.AbsoluteSize.X / frame.Size.X.Scale)),
                        startPos.X.Offset + delta.X,
                        startPos.Y.Scale + (delta.Y / (frame.AbsoluteSize.Y / frame.Size.Y.Scale)),
                        startPos.Y.Offset + delta.Y
                    )
                end
            end
        end
        
        local function onInputEnded(input)
            if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
                dragStart = nil
                startPos = nil
            end
        end
        
        button.InputBegan:Connect(onInputBegan)
        button.InputChanged:Connect(onInputChanged)
        button.InputEnded:Connect(onInputEnded)
        frame.InputBegan:Connect(onInputBegan)
        frame.InputChanged:Connect(onInputChanged)
        frame.InputEnded:Connect(onInputEnded)
    end
    
    makeDraggable(mainFrame, titleBar)
    makeDraggable(floatingBtn, floatingBtn)
    
    -- FUNÇÕES DOS BOTÕES
    closeBtn.MouseButton1Click:Connect(function()
        mainFrame.Visible = false
        isMenuOpen = false
    end)
    
    closeBtn.TouchTap:Connect(function()
        mainFrame.Visible = false
        isMenuOpen = false
    end)
    
    toggleBtn.MouseButton1Click:Connect(function()
        toggleHat()
        if isHatEquipped then
            toggleBtn.Text = "🔴 REMOVER CHAPÉU"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(255, 200, 50)
        else
            toggleBtn.Text = "🟢 EQUIPAR CHAPÉU"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        end
    end)
    
    toggleBtn.TouchTap:Connect(function()
        toggleHat()
        if isHatEquipped then
            toggleBtn.Text = "🔴 REMOVER CHAPÉU"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(255, 200, 50)
        else
            toggleBtn.Text = "🟢 EQUIPAR CHAPÉU"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        end
    end)
    
    floatingBtn.MouseButton1Click:Connect(function()
        isMenuOpen = not isMenuOpen
        mainFrame.Visible = isMenuOpen
        if isMenuOpen then
            mainFrame.Position = UDim2.new(0.5, -160, 0.5, -225)
        end
    end)
    
    floatingBtn.TouchTap:Connect(function()
        isMenuOpen = not isMenuOpen
        mainFrame.Visible = isMenuOpen
        if isMenuOpen then
            mainFrame.Position = UDim2.new(0.5, -160, 0.5, -225)
        end
    end)
    
    return screenGui, toggleBtn, floatingBtn
end

-- CRIAR GUI
local gui, toggleBtn, floatingBtn = createMobileGUI()

-- EQUIPAR AUTOMATICAMENTE
task.wait(0.5)
equipHat()
toggleBtn.Text = "🔴 REMOVER CHAPÉU"
toggleBtn.BackgroundColor3 = Color3.fromRGB(255, 200, 50)

-- MANTÉM O CHAPÉU NA CABEÇA QUANDO O PERSONAGEM SE MOVE
RunService.Heartbeat:Connect(function()
    if isHatEquipped and hatModel and head then
        local handle = hatModel:FindFirstChild("Handle")
        if not handle then
            handle = hatModel:FindFirstChildWhichIsA("BasePart")
        end
        
        if handle then
            local headCFrame = head.CFrame
            local hatCFrame = headCFrame * CFrame.new(0, 1.2, 0)
            handle.CFrame = hatCFrame
        end
    end
end)

-- RE-EQUIPAR AO RENASCER
player.CharacterAdded:Connect(function(newChar)
    character = newChar
    head = character:WaitForChild("Head")
    task.wait(0.5)
    
    if isHatEquipped then
        removeHat()
        equipHat()
    end
end)
